{
  "id": "zenmux",
  "name": "ZenMux",
  "description": "ZenMux LLM provider — OpenAI-compatible aggregator with 200+ models",
  "version": "0.1.2",
  "providers": ["zenmux"],
  "modelSupport": {
    "modelPrefixes": ["zenmux/"]
  },
  "providerAuthEnvVars": {
    "zenmux": ["ZENMUX_API_KEY"]
  },
  "setup": {
    "providers": [
      {
        "id": "zenmux",
        "authMethods": ["api-key"],
        "envVars": ["ZENMUX_API_KEY"]
      }
    ]
  },
  "providerAuthChoices": [
    {
      "provider": "zenmux",
      "method": "api-key",
      "choiceId": "zenmux-api-key",
      "choiceLabel": "ZenMux API key",
      "groupId": "zenmux",
      "groupLabel": "ZenMux",
      "groupHint": "API key",
      "optionKey": "zenmuxApiKey",
      "cliFlag": "--zenmux-api-key",
      "cliOption": "--zenmux-api-key <key>",
      "cliDescription": "ZenMux API key"
    }
  ],
  "uiHints": {
    "apiKey": {
      "label": "ZenMux API key",
      "placeholder": "zm-...",
      "sensitive": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  }
}
